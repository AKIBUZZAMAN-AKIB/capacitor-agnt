<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background wakes: a periodic JobScheduler job that rebuilds the engine
         headlessly and runs the tasks the engine left in scheduler.json. The
         service is bound by the system (BIND_JOB_SERVICE), never by other apps.
         NOTE: no RECEIVE_BOOT_COMPLETED and therefore no setPersisted(true) —
         the app re-arms the job when it next runs instead of pretending to
         survive a reboot it does not handle. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name="com.t6x.plugins.phonebuddy.PhoneBuddyWakeService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>
</manifest>
